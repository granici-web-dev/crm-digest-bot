# План и состояние

Обновляется в конце каждой сессии: что сделано, что дальше, какие решения приняты. История в git, здесь только текущее.

## Где мы (25.09.2026)

- Сделано: стандарты (PRINCIPLES, STACK, TESTING), ADR-001 accepted, .env.example, settings.json, разведка mefi API (docs/mefi-api-notes.md), фикстура tests/fixtures/mefi/search_3_leads.json, конфиги исправлены по живым данным.
- Фундамент закрыт (сессия 3): craft 267f27f, 44f81c1; critique; дополнение к shape d996160; harden тремя коммитами: 41f2a4c (персональные данные, pre-commit), f9b73ff (терпимая модель лида, nullable is_duplicate, проверка полноты снапшота), 91db89e (устойчивость прогона, Retry-After через пейсер, время по Бухаресту и DST). 55 тестов зелёные, pre-commit чистый.
- Сессия 4, часть 1: эталон из SB KPi.xlsx (`scripts/build_etalon.py` → `tests/fixtures/etalon-2026-05.json`, 314 лидов, 6 консультантов, проверка на контакты клиента), `docs/kpi-definitions.md` переписан по формулам workbook, `config/kpi.yaml`, ADR-002 (v1 повторяет Excel). Speed-to-lead теперь ADR-003.
- Сессия 4, часть 2 (25.09.2026): SB KPi.xlsx составлен через ИИ, ADR-002 переписан (`ADR-002-kpi-brief-primary.md`): формулы по брифу, SPI предварительный и скрыт. Эталон пересобран по формулам брифа, значения Excel в `excel_reference`, Moaca Andreea сверена вручную (`meta.manual_check`). m6 выключен.
- Сессия 4, часть 3 (25.09.2026): metrics/ по shape `docs/shapes/2026-09-25-metrics.md` через tdd, коммиты 6cbccb6..907b0c8. `frame.py` (load_lead_frame, prepare_lead_frame, unknown_manager_ids), `kpi.py` (счётчики и 9 KPI по компании, шоуруму, консультанту), `spi.py` (provisional), `cockpit.py` (m5), `extra.py` (revenire, когорта, дельта). `test_kpi_matches_etalon` зелёный, 151 тест.
- Сессия 4, часть 4 (25.09.2026): critique metrics/ (28 замечаний), решения в дополнении к shape; harden пятью коммитами: 84f7fbf (громкий отказ: нет снапшота → SnapshotMissingError, ключи причин и PARTNERSHIP обязательны в конфиге, LEADS/USEFUL по флагам excluded_from_*, naive-время → ошибка, границы порогов), 824175d (provisional структурно: requires_kpi_status у m6 плюс валидатор AppConfig, тест импортов spi), 2660141 (цели ссылаются на имена порогов, полнота целей и ступеней, монотонность, срок 14 дней только в kpi.yaml), dc88cac (LeadCounts.unmapped, эталон сверяет итог по компании и разрез по шоурумам), b86af9f (исключение для ключей причин в PRINCIPLES), d4bb0b0 (load_lead_frame в src/digest/db/lead_frame.py, ссылки на kpi-definitions у формул, DST-тесты), 885034e (overdue_revenire отдаёт id лидов, счётчики m5 остаются int). 211 тестов зелёные, pre-commit чистый.
- Сессия 5 (25.09.2026): ядро доставки по shape `docs/shapes/2026-09-25-delivery.md` через craft, три коммита: 62cc137 (settings, report_period, split_message по 4096 UTF-16, повтор flood wait, notify_ops), 371d3b2 (metrics/daily.py, шаблоны d1 RO/RU, syrupy), bbfdc95 (runner, app с APScheduler, `python -m digest report daily --date --dry-run`, засев schedules и report_language). 260 тестов зелёные, pre-commit чистый. Реальная отправка в Telegram не проверялась: нет токенов в окружении, только фейковая сессия aiogram.
- Сессия 5, critique и harden (25.09.2026): 23 замечания, решения в «Дополнении после critique» shape доставки. Четыре коммита: 81b20cb (в логах только describe_error, при dry-run в ReportDeps нет продового chat_id, group/test/ops chat_id попарно различны), f3f0ef5 (daily_window и правило «снапшот ровно за вчера» внутри metrics/daily.py, новый лид = created_at не раньше начала окна, старые пропавшие лиды и неизвестные источники → алерт через ModuleResult.alerts, sources.other явно в Alte), 45b1ae7 (дата снапшота от period.end, повтор не затирает message_ids, свежий running с алертом, реестр модулей через ReportDeps), затем коммит app: `python -m digest snapshot` (без --date: mefi отдаёт только текущее состояние), JSON-логи, docker-compose.yml и README «Локальный прогон», шоурумы Brașov, București, Cluj, сноска источника B одна на отчёт. 276 тестов зелёные, pre-commit чистый.
- Сессия 6 (25.09.2026): d2–d6 и долги первого прогона по shape `docs/shapes/2026-09-25-daily-d2-d6.md` (f28a517) через craft, три коммита: 5d51519 (BIFE 2026 в other, FacebookMessanger в web, `not_taken` у id 7, params d2/d5 проверяются при загрузке, алерт UNMAPPED делит «без статуса» и «неизвестный статус», алерт unknown_raw_key в раннере, alembic через DatabaseSettings только с DATABASE_URL), 9013c2c (`metrics/daily_checks.py`, revenire строго `< today`, ADR-004, раздел «Ежедневные проверки» в kpi-definitions), eabc898 (модули, шаблоны RO/RU, syrupy, интеграционный тест d1..d6). 355 тестов зелёные, pre-commit чистый. На живом снапшоте не прогонялось: в сессии не было доступа к `.env`.
- Сессия 6, critique и harden (25.09.2026): critique двумя осями, все замечания приняты, решения в «Дополнении после critique» shape d2–d6 (ffc5c48). Четыре коммита: b26d410 (ACTIVE_OFFERS_14 только у лидов ACTIVE/ACTIVE_FOLLOWUP, ADR-005, эталон пересобран: изменились только active_offers_14 и acr, у Moaca Andreea 6 → 0), 54d1795 (итог и общий максимум d2/d3 из metrics/, раздельные dataclass по единицам, `comparable_previous` для d1 и d4, дельта d4 при нуле, макрос «не взят»), 6241d93 (касание по контакту при сдвиге > touch_tolerance_seconds = 60 с, пустой и отрицательный = не касались, created_by в unknown_manager_ids, unknown_raw_key только новые ключи, заголовок d2 «без касания или не взятые», пустой кадр, порядок d5, DST-тесты d2), затем refactor (params разобраны один раз в ModuleRegistry, валидаторы разделены, модуль с params обязан быть в реестре, site_average_days в params d5, «лидов: N» в алерте). 381 тест зелёный, pre-commit чистый.
- Отступления от shape d2–d6: (1) `created_by_id` читается из `raw.created_by.id` в SQL `load_lead_frame`, а не в `prepare_lead_frame` (он получает строки без raw); тестовые строки снапшота кладут его в raw через `lead_snapshots_row`. (2) «Только Site» для d5 задан как `sources.site: ["Site"]` в status-mapping.yaml (подмножество web, валидатор), а не литералом в коде. (3) Тест `test_module_disabled_in_module_settings_is_skipped` теперь ждёт отправку без блока d1: раньше d1 был единственным модулем. (4) Интеграционные тесты в `tests/integration/test_report_runner.py`, а не `test_runner.py`. (5) DST-тест d6 сравнивает 25.10 с 18.10 (окно 25.10 длится 25 часов), а не 26.10 с 19.10: окно 26.10 переход не пересекает. (6) d4 при нуле висящих оферт печатает дельту только ненулевую: «nu (față de ieri: -2)»; при дельте 0 или «—» строка «nu» без скобок. (7) RO-подпись окна d5 «media ultimelor N zile» без числительного «de zile»: при N ≥ 20 форма будет неверной.
- Сессия 7 (25.09.2026): d1 компактнее (f3fb045, без пустых строк внутри блока шоурума). Деплой по shape `docs/shapes/2026-09-25-deploy.md` через craft, четыре коммита: 291de2e (Dockerfile на python:3.12-slim + uv, editable-установка ради CONFIG_DIR/TEMPLATES_DIR, uid 10001, entrypoint с alembic upgrade head; `docker-compose.prod.yml` с отдельным именем проекта, app/postgres/backup, лимиты логов; `deploy/backup.sh` ежечасно проверяет дамп за сегодня по UTC, хранение 30 дней, 5 тестов), 731737d (стартовое сообщение «Бот запущен, версия <sha>, DRY_RUN=<0|1>.», проверка свежести дампа после daily, порог 26 ч), 90f31e5 (CI: ruff, mypy, TODO-хук, pytest, docker build), затем docs (`docs/deploy.md`, README, .env.example, STACK). 393 теста зелёные. Образ собран локально (uid, APP_VERSION, пути config/templates проверены), smoke-тест compose на ноутбуке не запускался: его делает пользователь.
- Сессия 7, critique и harden деплоя (25.09.2026): 25 замечаний, все приняты, решения в «Дополнении после critique» shape деплоя. Два коммита: fb73496 (сбой проверки свежести дампа уходит в служебный бот, тесты проводки report_job, `init: true`, монтирование `deploy/` каталогом, `.partial` в ротации, подсказка COMPOSE_FILE, CI: push только main, checkout по sha, import digest.app в образе, python:3.12.14-slim), затем docs (`docs/deploy.md` переписан: раздел 0 с источниками `.env`, заказ CPX12, перенос базы после первого up с `--single-transaction --exit-on-error` и сверкой count, обновление по шагам с дампом `before-<OLD_SHA>`, проверка reboot, переход на прод). 397 тестов зелёные, pre-commit чистый, образ собран и `import digest.app` в нём проверен. Инструкция руками не прогонялась.
- Сессия 8 (25.09.2026): недельный отчёт по shape `docs/shapes/2026-09-25-weekly.md` через craft, три коммита: 59d5946 (`metrics/weekly.py`: рабочий день лида, неделя лидов Пн–Вс по рабочему дню, окно недели = семь ежедневных окон, таблицы w1, визиты w2, воронка w3 через `lead_counts`, потери w4, w8; раздел «Недельные окна» в kpi-definitions; правило рабочей недели в PLAN и CLAUDE.md), 8c980bb (`ModuleResult.document`, `send_document_with_retry`, `ReportContext.week_ago` = success-снапшот ровно за S−7, документы уходят после текста, их message_id в report_runs), затем коммит модулей w1–w4, w8, w12 (шаблоны RO/RU, таблицы строками `<code>`, Excel через xlsxwriter: три листа ручного отчёта, «Lead-uri» и «Vizite» по восемь колонок, syrupy, openpyxl). 450 тестов зелёные. На живом снапшоте не прогонялось: в сессии нет `.env`.
- Сессия 8, critique и harden (25.09.2026): critique двумя изолированными осями, 26 замечаний, все приняты, решения в «Дополнении после critique» shape недельного отчёта. Два коммита: 8b74a61 (имя Excel `<tenant_id>_sapt<неделя>_<год>.xlsx` через `ReportContext.tenant_id`; `tenant_display_name` в status-mapping.yaml для шапки отчёта и d1; рабочие часы в подписях из `working_hours`; имена источников визитов из `sources.showroom_visit`; `label_ro`/`label_ru` у причин LOST, обязательные; прошлое 0 → «—»; лист «Vizite» с колонкой «Zi»; минус U+2212), b41f3c2 (итоги и ранжирование причин в dataclass metrics; ячейки Excel строит модуль, metrics отдаёт сырые `LEAD_ROW_COLUMNS`; снапшот S−7 грузится только если запускается w8, метрика доверяет контексту; день визита сверен с `daily_window`; тесты: синтетические данные клиента в кадре не доходят ни до одной ячейки Excel, явные assert чисел w3/w8, переход на летнее время 29.03 и окно визитов при переводе часов). 468 тестов зелёные, pre-commit чистый.
- Отступления от shape недельного отчёта, подтверждённые: названия дней недели RO и подписи таблиц в Python (`render.RO_WEEKDAYS`, `reports/modules/weekly.py`), их читают шаблон и Excel. Заголовки листов Excel без длинного тире. Подписи с числом без склонений («Lead-uri 6 → Utile 6»).
- Сессия 9 (25.09.2026): месячный отчёт по shape `docs/shapes/2026-09-25-monthly.md` через craft. Коммиты: 3e77f06 (refactor: `loss_reasons_in_window` общий для w4 и m8, вместе с shape), ed26ed5 (`metrics/monthly.py`: окно месяца, воронка, уровни SCR по `params.levels` m4, тренд 6 месяцев, потери к прошлому месяцу, строки лидов; `reports/charts.py` на matplotlib Figure без pyplot; `templates/charts.<lang>.yaml`; раздел «Месячное окно» в kpi-definitions), aea7fd8 (refactor: общий повтор flood wait в delivery), 81f6b27 (`ModuleResult.photo`, `send_photo_with_retry`, порядок текст → фото → документы, id в report_runs), a86bb75 (модули m2–m5, m8, m19, шаблоны RO/RU, Excel из четырёх листов, syrupy, интеграционный monthly-прогон). 521 тест зелёный, pre-commit чистый. На живом снапшоте не прогонялось, картинки проверены глазами на синтетических данных.
- Отступления от shape месячного отчёта: (1) отдельный коммит refactor(delivery): третий повтор цикла flood wait (текст, документ, фото), правило трёх. (2) Фильтр `percent1` (одна десятая) в m4 и m5: целое округление показало бы 9,6 % как 10 % рядом с целью 10 %. (3) Порядок причин m8 (`reasons_by_count`, причина с нулём в этом месяце и ненулём в прошлом остаётся) и `reason_share` добавлены в `MonthlyLossReasons` в коммите модулей. (4) Названия месяцев для текстов m3, m8 и листа «Motive pierdere» берутся из `templates/charts.<lang>.yaml`. (5) Тесты Excel m19 в `tests/unit/test_monthly_render.py`, а не в отдельном `test_monthly_excel.py`. (6) m2, m4 и лист «Funnel showroom» показывают только шоурумы с лидами за месяц. (7) Тест «нет реализованных модулей» переведён с monthly на yearly.
- Сейчас: smoke-тест `docker-compose.prod.yml` на ноутбуке (пользователь), затем деплой по `docs/deploy.md` и неделя DRY_RUN=1 в тестовой группе с d1–d6 и weekly (понедельник 09:00). На неделе DRY_RUN сверить w1 с ручным понедельничным отчётом.
- На неделе DRY_RUN сверить с владельцем: новый лид Showroom ∧ ACTIVE_FOLLOWUP считается и в Alte/Showroom (revenire), и в Vizita.
- Daily реализован целиком (d1–d6; d7, d8 выключены). Weekly: w1–w4, w8, w12 (w5–w7, w9–w11 выключены). Monthly: m2–m5, m8, m19 (остальные выключены). Yearly без реализованных модулей не отправляется (report_runs failed, алерт).
- Логи JSON (`digest/log_format.py`): уровень лога в ключе severity, потому что level в extra это уровень отчёта; у исключений только тип и стек, без текста.
- Отступления от shape доставки (п. 23 critique): тесты в `test_metrics_daily.py`, а не `test_daily.py`; вместо чистой `enabled_modules(level, config, overrides)` асинхронная `runnable_modules(deps, level)`, читающая module_settings; вместо `send_text` цикл в раннере с `send_message_with_retry`, чтобы писать message_ids после каждой части; реестр называется `IMPLEMENTED_MODULES` и передаётся через `ReportDeps.modules`; `ReportContext` = report_date, previous (`PreviousSnapshot`), config, language; шоурумы вне списка config получают свой блок после config-шоурумов; итоговая строка TOTAL · Leads · Vizite · Oferte · Contracte; CLI `report` принимает все четыре уровня и засевает дефолты; длина сообщения считается в UTF-16, строка длиннее лимита → MessageLineTooLong; валидатор showroom_visit_status.
- Расхождение с дополнением к shape: там сказано, что 5 от API и 4 записанных при порогах по умолчанию это failed, но по записанным там же порогам (max(5, 0.5 %) недополучено, max(10, 1 %) пропущено) это success. Реализованы пороги; тест переименован в test_snapshot_below_thresholds_is_success_with_alert_data. Если 5/4 должно падать, пороги в status-mapping.yaml нужно ужесточить.
- Незнакомые ключи лида (unknown_raw_key) пишутся в raw, не вырезаются; алерт шлёт раннер отчёта, только по ключам, которых не было в предыдущем успешном снапшоте. Остальные записи custom_field_mismatches (missing, unexpected_value, invalid_shape) в служебный бот пока не уходят.
- Локально тесты идут с `TESTCONTAINERS_RYUK_DISABLED=true`: docker pull образа ryuk зависает.
- Дальше: /settings по docs/first-sessions.md.

## Принятые решения (не обсуждать заново)

- Недельное правило рабочего окна: у каждого лида свой рабочий день: время в [10:00, 19:00) по Бухаресту → этот день, любое другое → следующий календарный день. Неделя лидов = лиды с рабочим днём Пн–Вс, это не интервал created_at: воскресенье 00:00–10:00 уходит в понедельник следующей недели, прошлое воскресенье 00:00–10:00 входит в этот понедельник. Источник Showroom из недельного счёта исключён; проверено по ручным отчётам за июль 2026. Визиты, контракты, потери и воронка недели считаются по окну [Вс 19:00, Вс 19:00) = семь ежедневных окон (shape `docs/shapes/2026-09-25-weekly.md`).
- KPI по брифу §3 (ADR-002): PARTNERSHIP и тестовые аккаунты вне LEADS, SC = клиенты среди визитов, деление на ноль → null («—»), ACR по last_contact_at. SB KPi.xlsx только тестовые данные, его ошибка ACR (ссылка на пустую B2) не воспроизводится.
- Баллы, штраф IRR, SPI, уровни, рекомендации предварительные: в config/kpi.yaml (status: provisional) и в metrics/, но не в отчётах MVP. m5 = 9 KPI с целями без SPI, m6 выключен.
- Только ежедневный снапшот в 19:00, повтор 19:10. Частый опрос и speed-to-lead не в v1, w5 выключен, формула через ADR-003.
- Фильтры дат mefi не используются, окна считаются локально по снапшоту в Europe/Bucharest.
- Lifecycle mefi для категорий не используется, только status.name. status null → UNMAPPED. Алерт UNMAPPED только при первом появлении id.
- Исчезнувшие лиды: счётчик missing_since_previous в snapshot_runs (без пропущенных сегодня), флага в схеме нет.
- success = снапшот полный: пороги полноты в секции snapshot status-mapping.yaml. is_duplicate nullable, NULL = неизвестно; метрики с исключением дублей падают на NULL в окне.
- raw jsonb без контактов клиента (список raw_strip в status-mapping.yaml), textarea-поля хранятся.
- Консультанты: config/managers.yaml, сверен со списком пользователей mefi 24.09.2026.
- Метрики по консультантам считают только active: true; лид с assigned_to.id вне managers.yaml находит unknown_manager_ids (metrics/ не логирует и не ходит в БД), алерт шлёт раннер отчёта (сессия 5).
- Срок «лид ACTIVE без движения» по status_changed_at получит свой ключ stale_lead_days в kpi.yaml, когда появится модуль, который его читает (сессия 5).
- m6 и любой модуль с requires_kpi_status: calibrated не включается, пока kpi.yaml в status: provisional; /settings (сессия 6) использует тот же валидатор AppConfig.
- Лиды с assigned_to.id консультанта с test_account: true исключаются из всех метрик в prepare_lead_frame.
- Отчёты читают только снапшот с snapshot_date = today и status = success, иначе пометка «данные mefi недоступны».
- Висящая оферта (ACTIVE_OFFERS_14, ACR, d4) только у лида ACTIVE или ACTIVE_FOLLOWUP (ADR-005): 564 висящие на живом снапшоте 25.09.2026 были почти все у LOST.
- d3: просрочка строго `Data revenire < today`, совпадение с днём создания не отсекается (ADR-004).
- d2: касание = смена статуса, `last_contact_at − created_at > touch_tolerance_seconds` (60 с; пустой или раньше создания = не касались), ofertat или лид создан консультантом с `active: true`; `created_by.id` вне managers.yaml идёт в общий алерт с `assigned_to.id`; лид на `not_taken` или без консультанта «не взят» независимо от касаний. Условие «статус пуст» не используется: IN PROCES статус по умолчанию у лидов из API.
- d5 «сайт молчит» только по `sources.site`, порог site_zero_min_average 2; всплеск IRELEVANT от 5. d6 считает контракты по converted_at, d1 по разнице снапшотов (ADR-004).
- m19 в MVP только Excel, PDF на этапе 2.
- Все окна отчётов заканчиваются в 19:00 снапшота. Месяц = [последний день прошлого месяца 19:00, последний день месяца 19:00), ежедневные окна всех дней месяца (подтверждено пользователем 25.09.2026, kpi-definitions «Месячное окно»).
- m3: только лиды (created_at) и контракты (converted_at), без процента; когортная конверсия остаётся модулю m9. m4: шоурумы и компания, консультанты только в m5. В Excel m19 колонка дня на листе «Lead-uri luna» подписана «Zi».
- w8: у оферт нет процента к прошлой неделе, только число за неделю или «—» (подтверждено пользователем 25.09.2026). Изменение к прошлой неделе: прошлое значение 0 даёт «—» всегда, в том числе 0 против 0.
- Подписи причин потерь: `label_ro`/`label_ru` у причины в status-mapping.yaml; название тенанта в текстах из `tenant_display_name`, имя вложения из TENANT_ID.
- Лид на assigned_to.id = 7 (Marketing Sofa) считается не взятым в работу; использовать в d2.
- d1 сравнивает только со снапшотом строго за вчера; при пропуске снапшота строки Vizita/Oferta/Contract за тот день теряются, недельный отчёт их покроет.
- Строки Leads d1 взаимоисключающие: PARTNERSHIP или источник Colaborare → Designer/Colaboratori; sources.other, Showroom ∧ ACTIVE_FOLLOWUP и источник вне всех групп → Alte/Showroom (revenire); затем web, Telefon, WhatsApp. Showroom вне ACTIVE_FOLLOWUP это визит (Vizita), не лид. «Leads N» в итоге = сумма пяти строк.
- RU-шаблон d1: подписи строк румынские, переводятся шапка, пометки и сноска. Заголовок шоурума как в mefi (`Brașov`).
- report_runs: success/partial не отправляются повторно; failed отправляется заново; running старше 30 минут = прерван, отправляется целиком с алертом (дубль лучше пропуска); running моложе 30 минут не трогается.
- Источник B на этапе 2 читается через POST /admin/<модуль>/table с length=5000, не через обход HTML. Вход в mefi это блокер: reCAPTCHA плюс обязательная 2FA; цель официальный API, временная мера ручной вход с cookie.

## Ждём извне

- Кто такой Doja Ovidiu (Cluj, 18 лидов в эталоне), нет в пользователях mefi.
- Письмо директора в mefi про API для Oferte/Contracte/Facturi, теперь условие этапа 2.
- Калибровка SPI с владельцем после 2–3 месяцев снапшотов (плюс Excel 2024–2025), отдельным ADR.

## Правило контекста

Между сессиями /clear. Перед craft план должен быть в docs/shapes/. В конце сессии обновить этот файл и закоммитить вместе с работой.
