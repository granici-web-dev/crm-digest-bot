{% macro reasons(by_reason) %}{% for reason in losses.reasons_by_count if by_reason[reason] %}{{ reason_labels[reason] }} {{ by_reason[reason] }}{% if not loop.last %} · {% endif %}{% endfor %}{% endmacro %}
{% if losses.total %}
<b>Потеряно {{ losses.total }}</b>: {% for reason in losses.reasons_by_count %}{{ reason_labels[reason] }} {{ losses.reason_total(reason) }}{% if not loop.last %} · {% endif %}{% endfor %}

{% for showroom, by_reason in losses.by_showroom.items() if losses.showroom_total(showroom) %}
{{ showroom or without_showroom }} {{ losses.showroom_total(showroom) }}: {{ reasons(by_reason) }}
{% endfor %}
{% else %}
<b>Потеряно: 0</b>
{% endif %}
