<b>Săpt. {{ week_number }} ({{ week_range }}) · Lead-uri fără {{ excluded_sources }}: {{ tables.total }}</b>
<i>Ore de lucru {{ working_hours }}: lead-urile din afara lor trec pe ziua următoare.</i>
{% for row in day_showroom_rows %}
<code>{{ row }}</code>
{% endfor %}

{% for row in showroom_source_rows %}
<code>{{ row }}</code>
{% endfor %}
